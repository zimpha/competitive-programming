#include <cstdio>
#include <vector>
#include <algorithm>

const int N = 5e5 + 10;

int main() {
  int n, q;
  scanf("%d%d", &n, &q);
  std::vector<int> p(n);
  for (int i = 0; i < n; ++i) scanf("%d", &p[i]);
  return 0;
}
